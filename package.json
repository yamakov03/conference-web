{
  "name": "conference-webrtc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd server/model && python app.py\" \"cd client && npm start\" \"cd server && npm start\"",
    "install:main": "npm install && cd server && npm install && cd ../client && npm install && cd ../server/model && pip install -r requirements.txt"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "mongodb": "^6.5.0"
  }
}
